from setuptools import setup
import os

# version_py = os.path.join(os.path.dirname(__file__), "src/torchheat", "version.py")
# version = open(version_py).read().strip().split("=")[-1].replace('"', "").strip()

setup()
