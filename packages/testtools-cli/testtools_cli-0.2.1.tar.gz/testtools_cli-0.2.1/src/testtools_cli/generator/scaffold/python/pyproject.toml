[project]
name = "{{ name }}"
version = "0.1.0"
description = "TODO"
authors = [
    {name = "TODO", email = "TODO"},
]
dependencies = [
    "testsolar-testtool-sdk>=0.2.1",
    "pydantic>=2.7.4",
    "loguru>=0.7.2",
]
requires-python = ">=3.9"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false

[tool.pdm.dev-dependencies]
dev = [
    "pytest-cov>=5.0.0",
    "pytest>=8.2.2",
    "ruff>=0.4.10",
    "mypy>=1.10.0",
    "setuptools>=70.1.0",
]
