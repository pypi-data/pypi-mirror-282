[tool.poetry]
name = "metavalidator"
version = "0.1.3"
description = "Flexible validation for meta data schema."
authors = ["KL <kun.lu.brain@gmail.com>"]
license = "BSD"
readme = "README.md"
packages = [{ include = "metastruct" }]

[tool.poetry.dependencies]
python = "^3.9"
pydantic = "^2.7.3"

[tool.poetry.group.dev.dependencies]
ruff = "^0.4.8"
pytest = "^8.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
