# Pycausalimpact

Python package for Geo-Matched Causal Impact (python wrapped version of R code)

## Installation

pip install my_package

## Setup
After first import, run setup() to install python wrapped R version of Causal Impact