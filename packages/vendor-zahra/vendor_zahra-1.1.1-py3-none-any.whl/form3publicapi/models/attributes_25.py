# -*- coding: utf-8 -*-

"""
form3publicapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from form3publicapi.api_helper import APIHelper


class Attributes25(object):

    """Implementation of the 'Attributes25' model.

    TODO: type model description here.

    Attributes:
        answer (AnswerEnum): Answer to direct debit request. Only `rejected`
            can be used.
        reason (str): Free text reason in addition to `reason_code`. Maximum
            length 105 characters for both inbound and outbound direct debit
            decisions.
        reason_code (str): Reason for a rejected decision. Required when
            answer is rejected, ignored otherwise. Has to be a valid SEPA
            direct debit decision reason code.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "answer": 'answer',
        "reason": 'reason',
        "reason_code": 'reason_code'
    }

    _optionals = [
        'answer',
        'reason',
        'reason_code',
    ]

    def __init__(self,
                 answer=APIHelper.SKIP,
                 reason=APIHelper.SKIP,
                 reason_code=APIHelper.SKIP):
        """Constructor for the Attributes25 class"""

        # Initialize members of the class
        if answer is not APIHelper.SKIP:
            self.answer = answer 
        if reason is not APIHelper.SKIP:
            self.reason = reason 
        if reason_code is not APIHelper.SKIP:
            self.reason_code = reason_code 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        answer = dictionary.get("answer") if dictionary.get("answer") else APIHelper.SKIP
        reason = dictionary.get("reason") if dictionary.get("reason") else APIHelper.SKIP
        reason_code = dictionary.get("reason_code") if dictionary.get("reason_code") else APIHelper.SKIP
        # Return an object of this model
        return cls(answer,
                   reason,
                   reason_code)
