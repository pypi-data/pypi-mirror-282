# -*- coding: utf-8 -*-

"""
form3publicapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

from apimatic_core.authentication.header_auth import HeaderAuth
from apimatic_core.utilities.auth_helper import AuthHelper


class Basic(HeaderAuth):

    @property
    def error_message(self):
        """Display error message on occurrence of authentication failure
        in Basic

        """
        return "Basic: Username or Password is undefined."

    def __init__(self, basic_credentials):
        auth_params = {}
        if basic_credentials is not None \
                and basic_credentials.username and basic_credentials.password:
            auth_params = {"Authorization": "Basic {}".format(
                AuthHelper.get_base64_encoded_value(basic_credentials.username, basic_credentials.password))}
        super().__init__(auth_params=auth_params)


class BasicCredentials:

    @property
    def username(self):
        return self._username

    @property
    def password(self):
        return self._password

    def __init__(self, username, password):
        if username is None:
            raise ValueError('username cannot be None')
        self._username = username
        if password is None:
            raise ValueError('password cannot be None')
        self._password = password

    def clone_with(self, username=None, password=None):
        return BasicCredentials(username or self.username,
                                password or self.password)
