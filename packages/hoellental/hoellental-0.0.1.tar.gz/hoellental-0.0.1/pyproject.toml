# BOILERPLATE
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

# PROJECT INFO
[project]
name = "hoellental"
version = "0.0.1"
description = "hoellental"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
